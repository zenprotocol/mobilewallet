import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  label: {
    paddingBottom: 10,
    fontSize: 18,
    color: "#e6e6e6",
    fontWeight: "400",
  },
  error: {
    color: 'red',
  },
});

export default styles;
